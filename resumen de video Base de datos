resumen de video Base de datos

las basesde datos siempre han existido, desde hace siglos.

los aspectos importantes de una base de datos es la escabilidad, luegola frecuencia de actualizacion y la velocidad.

una base de datos es para crear,organnizar, guardar y gestionar datos de manera libre, las operaciones crud son formas de poder guardar esa informacion.

sistema de gestion de bases de datos: tambien el motor de las bases de datos es importante, es el motor que hace que se mueva

existe tambien unos que son libres y otros que son de pago(los motores), otros son mas pesados, los cuales necesitan mas potencia de computo para poder ejecutarse pero con mas cosas.

SQL es el lenguaje de consultas general de todos los motores de DB.

Structured Query Lenguage

dentro de una base de datos hy mchas tablas

decimos, seleccione todo, columna filas con "SELECT *" FROM "autores"

los comandos aprendidos son SELECT, FROM,LIMIT.

si quedemos hacer consultas mas avanzadas podemos hacer WHERE (siempre sigue una tendencia de tipo buleana).

SELECT * FROM "lista_larga" WHERE "anio" = '2018' LIMIT 1

En SQL, la cláusula WHERE se usa para filtrar datos, permitiéndote extraer solo aquellas filas de una tabla que cumplen con una condición específica
